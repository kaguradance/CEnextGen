#include<stdio.h>
#include<string.h>

int palindrome(char s[]);

int main(){
    char text[21];
    scanf("%s",&text);
    palindrome(text);
}

int palindrome(char s[]){
    int i,re;
    for(i = 0;i < strlen(s); i++){
        printf("1");
        
    }

}

